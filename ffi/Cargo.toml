[package]
name = "webrtc-core-ffi"
version = "0.7.0"
edition = "2021"
authors = ["xbl1e"]
description = "C/C++ FFI bindings for webrtc-core"
license = "MIT"

[dependencies]
webrtc-core = { path = "..", version = "0.7.0" }
bytes = "1.4"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]
